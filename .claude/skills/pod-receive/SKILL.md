---
name: pod-receive
description: Receive and apply a Drop-Pod capsule on the work machine. Invoke on "pod", "drop-pod", "new pod", "apply the pod", "what landed", or whenever the user mentions a directory of the Drop-Pod repo.
---

# pod-receive

Read and follow `AGENTS.md` at the root of the Drop-Pod clone. It holds the pod contract
and the receiving procedure; this skill is only the Claude Code entry point to it.
